apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespaces.yaml
  - services.yaml
  - orchestration-api-deployment.yaml
  - orchestration-supervisor-deployment.yaml
  - execution-runners-deployments.yaml
  - rag-query-deployment.yaml
  - networkpolicy.yaml
  # ingress.yaml — not needed when using Socket Mode; re-add for Events API mode

