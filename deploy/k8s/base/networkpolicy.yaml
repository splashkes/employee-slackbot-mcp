# ---------------------------------------------------------------------------
# Default deny-all for both namespaces
# ---------------------------------------------------------------------------
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: artbattle-orchestration
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: artbattle-execution
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
---
# ---------------------------------------------------------------------------
# Orchestration ingress: allow external traffic (ingress controller) to
# orchestration-api on port 3000, and intra-namespace traffic to
# orchestration-supervisor on port 8080.
# ---------------------------------------------------------------------------
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: orchestration-api-ingress
  namespace: artbattle-orchestration
spec:
  podSelector:
    matchLabels:
      app: orchestration-api
  policyTypes:
    - Ingress
  ingress:
    - from: []
      ports:
        - protocol: TCP
          port: 3000
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: orchestration-supervisor-ingress
  namespace: artbattle-orchestration
spec:
  podSelector:
    matchLabels:
      app: orchestration-supervisor
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: artbattle-orchestration
      ports:
        - protocol: TCP
          port: 8081
---
# ---------------------------------------------------------------------------
# Orchestration internal: allow api -> supervisor on port 8081
# ---------------------------------------------------------------------------
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: orchestration-internal-egress
  namespace: artbattle-orchestration
spec:
  podSelector:
    matchLabels:
      app: orchestration-api
  policyTypes:
    - Egress
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: orchestration-supervisor
      ports:
        - protocol: TCP
          port: 8081
---
# ---------------------------------------------------------------------------
# Orchestration egress: allow HTTPS, DNS, and managed Redis (port 25061)
# ---------------------------------------------------------------------------
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: orchestration-egress-policy
  namespace: artbattle-orchestration
spec:
  podSelector:
    matchLabels:
      plane: orchestration
  policyTypes:
    - Egress
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - protocol: TCP
          port: 443
        - protocol: TCP
          port: 25061
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
---
# ---------------------------------------------------------------------------
# Execution egress: allow HTTPS, DNS, Postgres, and managed Redis (port 25061)
# ---------------------------------------------------------------------------
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: execution-egress-policy
  namespace: artbattle-execution
spec:
  podSelector:
    matchLabels:
      plane: execution
  policyTypes:
    - Egress
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - protocol: TCP
          port: 443
        - protocol: TCP
          port: 25061
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
        - protocol: TCP
          port: 5432
