# Tests

Starter test layout for `employee-slackbot`.

## Structure

- `unit/`: fast deterministic tests for local and CI PR gates
- `fixtures/`: static payloads for Slack, OpenAI, and MCP test cases

## Run

```bash
npm test
```
